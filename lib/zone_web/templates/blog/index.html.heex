<%= for post <- @posts do %>
  <div id={post.id} style="margin-bottom: 3rem;">
    <h2>
      <%= link post.title, to: Routes.blog_path(@conn, :show, post)%>
    </h2>

    <%= raw post.description %>
    <p>
      <time><%= post.date %></time> · <%= Enum.join(post.tags, ", ") %>
    </p>
  </div>
<% end %>
